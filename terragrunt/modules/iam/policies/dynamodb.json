{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": {
        "Service": "dynamodb.amazonaws.com"
      },
      "Action": "dynamodb:GetItem",
      "Resource": "arn:aws:dynamodb:${var.region}:${module.ou_creation.outputs.ou_id}:table/${module.dynamodb.outputs.table_name}"
    },
    {
      "Effect": "Allow",
      "Principal": {
        "Service": "dynamodb.amazonaws.com"
      },
      "Action": "dynamodb:PutItem",
      "Resource": "arn:aws:dynamodb:${var.region}:${module.ou_creation.outputs.ou_id}:table/${module.dynamodb.outputs.table_name}"
    }
  ]
}
